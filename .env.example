# Backend API base URL
# Leave empty to use mock data (for development/demo without backend)
VITE_API_BASE_URL=http://localhost:5000/api

# NOTE: Never put these in the frontend .env:
# GROQ_API_KEY=...        ← Backend only
# MONGODB_URI=...         ← Backend only
# JWT_SECRET=...          ← Backend only
